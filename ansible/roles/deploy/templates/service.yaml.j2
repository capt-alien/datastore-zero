apiVersion: v1
kind: Service
metadata:
  name: datastore-zero
  namespace: {{ target_namespace }}
spec:
  type: NodePort
  selector:
    app: datastore-zero
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30800
